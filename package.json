{
  "name": "recoverflow",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "vitest run",
    "prepare": "husky",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check:env": "dotenv -e .env -- tsx packages/shared/src/check-env.ts",
    "db:generate": "pnpm --filter @recoverflow/db generate",
    "db:migrate": "pnpm --filter @recoverflow/db migrate",
    "db:deploy": "pnpm --filter @recoverflow/db deploy",
    "db:studio": "pnpm --filter @recoverflow/db studio"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "^22.10.2",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.4.2",
    "tsx": "^4.19.2",
    "turbo": "^2.3.3",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.11",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.1",
    "vitest": "^2.1.8",
    "dotenv-cli": "^8.0.0"
  }
}
